import { createSlice } from "@reduxjs/toolkit";

const PagingSlice = createSlice({
    name : "PagingSlice",
    initialState : {
        pageno : 1,
    },
    reducers : {

    }
})

export default PagingSlice;